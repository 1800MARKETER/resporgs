flask>=3.0
duckdb>=1.0
pyarrow>=15.0
# python-whois is only needed to RUN scripts/enrich_whois.py locally.
# The droplet only reads the resulting parquet, so this doesn't need to be
# installed in production.
python-whois>=0.9
